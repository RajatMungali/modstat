// ============================================
// MODSTAT — Post creation (unused, kept for compatibility)
// ============================================

export const createPost = async () => {
  throw new Error('Use reddit.submitPost directly instead');
};
